name: Publish Fish Common API to GitHub Packages

on:
  push:
    branches: [ main ]
  release:
    types: [ created ]
  workflow_dispatch:

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven'
        server-id: github
        
    - name: Install Maven
      run: sudo apt-get install -y maven
      
    - name: Read project version
      id: version
      run: |
        VERSION=$(mvn -q -Dexec.executable=echo -Dexec.args='${project.version}' --non-recursive exec:exec)
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        
    - name: Build and Publish
      run: mvn -B clean deploy -DskipTests
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        GITHUB_REPOSITORY: ${{ github.repository }}
        
    - name: Generate Gradle Usage Snippet
      run: |
        echo "GRADLE_USAGE=repositories {
            maven {
                name = 'GitHubPackages'
                url = 'https://maven.pkg.github.com/${{ github.repository }}'
                credentials {
                    username = project.findProperty('gpr.user') ?: System.getenv('GPR_USER')
                    password = project.findProperty('gpr.key') ?: System.getenv('GPR_API_KEY')
                }
            }
        }
        
        dependencies {
            implementation 'com.fish:fish-common-api:${{ steps.version.outputs.version }}'
        }" >> $GITHUB_ENV
        
    - name: Create Release
      if: ${{ github.event_name == 'release' }}
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ steps.version.outputs.version }}
        name: Fish Common API v${{ steps.version.outputs.version }}
        body: |
          ### 🐟 Fish Common API v${{ steps.version.outputs.version }}
          
          This release contains the shared API for Fish plugins.
          
          ### 📦 Maven
          ```xml
          <dependency>
            <groupId>com.fish</groupId>
            <artifactId>fish-common-api</artifactId>
            <version>${{ steps.version.outputs.version }}</version>
          </dependency>
          ```
          
          ### 🐘 Gradle
          ```gradle
          ${{ env.GRADLE_USAGE }}
          ```
          
          ### 🔑 Authentication
          Add to `~/.gradle/gradle.properties`:
          ```
          gpr.user=<your-github-username>
          gpr.key=<your-github-token>
          ```
